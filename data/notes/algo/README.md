# Алгоритмы

Решения задач: контесты Яндекса (`yandex-*.py`), отдельные задачи, численные прикидки.
Каждый файл самостоятельный, между собой не импортируются — только стандартная библиотека.

Это архив решённого, а не библиотека. Приём из решения, оказавшийся общим, идёт в базу
приёмов, а не сюда.
